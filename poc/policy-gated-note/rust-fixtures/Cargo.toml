[package]
name = "miden-policy-note-fixtures"
version = "0.1.0"
edition = "2021"

[dependencies]
miden-client = { version = "0.15", features = ["testing", "tonic"] }
miden-client-sqlite-store = { version = "0.15", package = "miden-client-sqlite-store" }
tokio = { version = "1.48", features = ["rt-multi-thread", "macros", "fs"] }
